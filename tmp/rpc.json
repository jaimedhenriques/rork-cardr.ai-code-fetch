{"query":"create or replace function public.get_invitation_by_token(_token text) returns table(id uuid, email text, role text, expires_at timestamptz, accepted_at timestamptz, org_id uuid, org_name text) language sql security definer set search_path = public as $$ select i.id, i.email, i.role, i.expires_at, i.accepted_at, i.org_id, o.name as org_name from org_invitations i left join organizations o on o.id = i.org_id where i.token = _token; $$; grant execute on function public.get_invitation_by_token(text) to anon, authenticated; "}
